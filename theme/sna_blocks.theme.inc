<?php

function _sna_blocks_preprocess_sna_blocks(&$vars) {
  dsm($vars);
}

/**
 * The current element of the slideshow.
 *
 * @ingroup themeable
 */
function theme_sna_blocks_main_section($vars) {
  dsm($vars);
  return '<div>working here</div>';
}